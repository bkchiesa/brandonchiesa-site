---
import type { RoomId } from "../lib/site";

interface Props {
  id: RoomId;
}

const { id } = Astro.props;
---

<svg class="door-icon" viewBox="0 0 32 32" aria-hidden="true">
  {
    id === "work" && (
      <g fill="none" stroke="currentColor" stroke-width="1.4" stroke-linejoin="round">
        <rect x="5" y="12" width="22" height="14" rx="1.5" />
        <path d="M11 12 V10 a5 5 0 0 1 10 0 v2" />
        <path d="M5 17 H27" />
      </g>
    )
  }
  {
    id === "make" && (
      <g fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round">
        <path d="M11 9 L6 16 L11 23" />
        <path d="M21 9 L26 16 L21 23" />
        <path d="M18 8 L14 24" />
      </g>
    )
  }
  {
    id === "go" && (
      <g fill="none" stroke="currentColor" stroke-width="1.4">
        <rect x="4" y="9" width="24" height="14" rx="2" />
        <circle cx="11" cy="16" r="2" />
        <circle cx="21" cy="16" r="2" />
        <path d="M4 12 H28 M4 20 H28" />
      </g>
    )
  }
  {
    id === "roots" && (
      <g fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round">
        <circle cx="16" cy="7" r="2.2" />
        <circle cx="8" cy="20" r="2.2" />
        <circle cx="24" cy="20" r="2.2" />
        <circle cx="16" cy="26" r="2.2" />
        <path d="M16 9 V14 M16 14 L8 18 M16 14 L24 18 M12 22 L16 24 L20 22" />
      </g>
    )
  }
  {
    id === "now" && (
      <g fill="none" stroke="currentColor" stroke-width="1.4" stroke-linejoin="round">
        <path d="M9 6 H21 L25 10 V26 H9 Z" />
        <path d="M21 6 V10 H25" />
        <path d="M12 15 H20 M12 19 H18" />
      </g>
    )
  }
</svg>
